<nav class="navbar sticky-top navbar-expand-lg shadow-sm" style="background-color: #0c4c8a;">
  <div class="container">
    <a class="navbar-brand text-white fw-bold d-flex align-items-center" href="/">
      <i class="fas fa-laptop-code me-2"></i> Tech Dev
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <% if (locals.currentUser) { %>
          <li class="nav-item">
            <a class="nav-link text-white d-flex align-items-center" href="/empleados">
              <i class="fas fa-users me-1"></i> Empleados
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-white d-flex align-items-center" href="/empleados/calculator">
              <i class="fas fa-calculator me-1"></i> Calculadora
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-white d-flex align-items-center" href="/empleados/create">
              <i class="fas fa-user-plus me-1"></i> Crear Empleado
            </a>
          </li>
        <% } %>
      </ul>
      <ul class="navbar-nav mb-2 mb-lg-0">
        <% if (!locals.currentUser) { %>
          <li class="nav-item">
            <a class="nav-link text-white d-flex align-items-center" href="/login">
              <i class="fas fa-sign-in-alt me-1"></i> Iniciar sesión
            </a>
          </li>
        <% } else { %>
          <!-- <li class="nav-item">
            <a class="nav-link text-white d-flex align-items-center" href="/register">
              <i class="fas fa-user-edit me-1"></i> Crear Usuario
            </a>
          </li> -->
          <li class="nav-item">
            <a class="nav-link text-white d-flex align-items-center bg-danger bg-opacity-75 rounded px-2 ms-2" href="/logout">
              <i class="fas fa-sign-out-alt me-1"></i> Cerrar Sesión
            </a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>